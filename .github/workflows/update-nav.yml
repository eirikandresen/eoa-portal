name: Update NAV Data

on:
  schedule:
    # Kjører hver ukedag kl. 20:00 Oslo-tid (18:00 UTC)
    - cron: '0 18 * * 1-5'
  workflow_dispatch:  # Kjør manuelt fra Actions-fanen

jobs:
  update-nav:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests pandas openpyxl

      - name: Run update script
        run: python update_nav.py

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add nav_data.json
          git diff --staged --quiet || git commit -m "Auto-update NAV $(date +'%Y-%m-%d')"
          git push
